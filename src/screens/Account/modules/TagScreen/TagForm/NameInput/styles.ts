import { StyleSheet } from 'react-native';

export const useStyles = () => {
  return StyleSheet.create({
    input: {
      alignSelf: 'flex-end',
    },
    icon: {
      marginTop: 10,
    },
  });
};
