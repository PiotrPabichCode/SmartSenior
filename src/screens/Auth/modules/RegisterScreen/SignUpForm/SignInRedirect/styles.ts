import { StyleSheet } from 'react-native';

export const useStyles = () => {
  return StyleSheet.create({
    textLinks: {
      margin: 15,
      fontSize: 14,
      fontWeight: '600',
    },
    textRegister: {
      color: 'blue',
    },
  });
};
